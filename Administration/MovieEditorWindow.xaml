<Window x:Class="Administration.MovieEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Administration"
        xmlns:model="clr-namespace:DataAccess.Model;assembly=DataAccess"
        mc:Ignorable="d"
        Title="Новый фильм" Height="580" Width="714" ResizeMode="CanMinimize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="11"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="85"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Label Content="Название:" Margin="10,0" VerticalAlignment="Top" HorizontalContentAlignment="Right" Grid.Row="1" Height="26"/>
        <Label Content="Сюжет:" Margin="10,26,10,0" VerticalAlignment="Top" HorizontalContentAlignment="Right" Grid.Row="1" Height="26"/>
        <TextBox Text="{Binding Movie.Title}" Grid.Column="1" Height="23" Margin="10,3,10,0" TextWrapping="Wrap" VerticalAlignment="Top" Grid.Row="1" Grid.ColumnSpan="3"/>
        <TextBox Text="{Binding Movie.Plot}" Grid.Column="1" Height="100" Margin="10,31,10,0" TextWrapping="Wrap" VerticalAlignment="Top" Grid.Row="1" Grid.ColumnSpan="3"/>
        <ListBox ItemsSource="{Binding Genres}" Grid.Column="1" Height="100" Margin="10,136,10,0" Grid.Row="1" VerticalAlignment="Top" Grid.ColumnSpan="3">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel IsItemsHost="True" />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate DataType="model:Genre">
                    <CheckBox IsChecked="{Binding IsSelected}" Content="{Binding Name}" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Label Content="Жанр:" Margin="10,134,10,0" VerticalAlignment="Top" HorizontalContentAlignment="Right" Grid.Row="1" Height="26"/>
        <Calendar DisplayDate="{Binding Movie.ReleaseDate}" SelectedDate="{Binding Movie.ReleaseDate}" Grid.Column="1" HorizontalAlignment="Left" Height="171" Margin="7,270,0,0" Grid.Row="1" VerticalAlignment="Top" Width="183" FirstDayOfWeek="Monday"/>
        <Label Content="Дата выхода:" Margin="10,244,10,0" VerticalAlignment="Top" HorizontalContentAlignment="Right" Grid.Row="1" Height="26"/>
        <Label Content="Возраст:" Margin="10,244,10,0" VerticalAlignment="Top" HorizontalContentAlignment="Right" Grid.Row="1" Height="26" Grid.Column="2"/>
        <ComboBox ItemsSource="{Binding AgeLimits}" Grid.Column="3" Margin="10,248,10,0" Grid.Row="1" VerticalAlignment="Top" Height="22"/>
        <Label Content="Постер:" Margin="10,270,10,0" VerticalAlignment="Top" HorizontalContentAlignment="Right" Grid.Row="1" Height="26" Grid.Column="2"/>
        <Image Source="{Binding Movie.Poster}" Grid.Column="3" HorizontalAlignment="Left" Height="216" Margin="90,276,0,0" Grid.Row="1" VerticalAlignment="Top" Width="165"/>
        <Button Content="Обзор..." HorizontalAlignment="Left" VerticalAlignment="Top" Width="75" Grid.Column="3" Margin="10,276,0,0" Grid.Row="1"/>
        <Button Content="Сохранить" Grid.Column="3" Margin="0,0,10,10" Grid.Row="1" VerticalAlignment="Bottom" HorizontalAlignment="Right" Width="75" />
        <Button Content="Удалить" HorizontalAlignment="Left" VerticalAlignment="Top" Width="75" Grid.Column="3" Margin="10,301,0,0" Grid.Row="1"/>
        <Label Content="{Binding Movie.ReleaseDate, StringFormat='{}{0:d MMMM yyyy}'}" Margin="10,248,10,0" VerticalAlignment="Top" Grid.Row="1" Height="26" Grid.Column="1"/>
    </Grid>
</Window>
